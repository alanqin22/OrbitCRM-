"""Voice (browser speech-to-text) module.

Mints short-lived Azure Cognitive Services Speech tokens for the browser
SDK so the page can do high-quality STT in any Chromium browser (Edge,
Chrome, Brave) without depending on the browser's built-in Web Speech API.
"""
